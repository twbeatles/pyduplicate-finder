[pytest]
# Only collect tests from the dedicated test folder.
# The repo includes exploratory scripts under backup/ that may look like tests but
# are not meant to run in CI/dev by default.
testpaths = tests
norecursedirs = backup dist build __pycache__ .git .pytest_cache

